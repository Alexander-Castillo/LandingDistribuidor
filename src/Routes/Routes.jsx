import { BrowserRouter, Route, Routes } from "react-router-dom"
export const Rutas=()=>{
    return(
        <BrowserRouter>
            <Routes>
                
            </Routes>
        </BrowserRouter>
    );
};